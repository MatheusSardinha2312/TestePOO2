import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

class LeilaoService {
  static async listarLeiloes() {
  }

  static async obterLeilaoPorId(id: number) {
  }
}

export default LeilaoService;